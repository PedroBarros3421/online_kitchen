<%= render 'shared/page_title', title: "showing all recipes"%>

<div class="d-flex justify-content-center">
  <%= paginate @recipes%>
</div>

<%= render partial: 'recipe', locals: { obj: @recipes } %>


<div class="d-flex justify-content-center">
  <%= paginate @recipes%>
</div>